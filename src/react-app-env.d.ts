// eslint-disable-next-line
/// <reference types="react-scripts" />

interface CourseCard {
  testId: number;
  name: string;
  authorImg: string;
  courseImg: string;
  title: string;
  lessons: number;
  tasks: number;
  minutes: number;
  progress: number;
}
